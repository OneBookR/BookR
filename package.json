{
  "name": "bookr-calendar",
  "version": "1.0.0",
  "description": "Calendar comparison app",
  "main": "OneBookR/backend/server.js",
  "scripts": {
    "start": "cd OneBookR/backend && npm start",
    "build": "cd OneBookR/calendar-frontend && npm run build",
    "install-deps": "cd OneBookR/backend && npm install && cd ../calendar-frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}